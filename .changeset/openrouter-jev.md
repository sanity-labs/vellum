---
'@sanity-labs/vellum': minor
---

The server reaches Jev through OpenRouter when `OPENROUTER_API_KEY` is set, and through TypeSafe with `TYPESAFE_API_KEY` as before. OpenRouter wins when both are set.
